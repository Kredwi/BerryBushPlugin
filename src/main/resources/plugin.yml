name: ${project.name}
description: ${project.description}
author: ${plugin.author}
main: ${plugin.main-class}
license: MIT
version: ${project.version}
api-version: 1.14
softdepend: [ WorldGuard, Vault ]
permissions:
  bbush.fast:
    description: Access to fast growing
    default: op
  bbush.access:
    description: Access to bush
    default: true